# Changelog

## v0.4.0（2026-09-08）

- fix: 数据目录固定到 `%ProgramData%\LocalMap`，SYSTEM 计划任务与用户态引擎共享同一份数据；旧版 exe 同级 `data\` 在首次启动时自动迁移（旧目录保留作备份）。修复多套安装并存时映射"消失"（#4）
- fix: 80/443/20190 端口绑定失败即退出并明确提示，杜绝双实例并存抢端口（#4）
- fix: `/api/state` 返回开机自启任务指向的 exe 路径，与当前引擎不一致时管理页红色告警并可一键修复（#4）
- fix: HTTPS 已启用但 CA 未被系统信任时（数据目录迁移/重装后 CA 更换），`/api/https/enable` 幂等补装信任，管理页新增"修复信任"按钮（#4）
- fix: SYSTEM（session 0）下代理绕过列表变更后，改为以登录用户身份跑一次性交互式计划任务广播 `UpdatePerUserSystemParameters`，浏览器无需重启即可重读（#4）
- feat: 新增 `GET /api/selfcheck` 端到端自检（端口归属 / HTTPS 证书 / CA 信任 / hosts / 代理绕过 / 开机自启指向），管理页"自检"面板展示逐项结果与修复提示（#4）
- fix: NSIS 安装/卸载前结束残留的 localmap-engine.exe 并删除指向旧路径的开机自启任务（#4）

## v0.3.0（2026-09-04）

- feat: 映射域名自动写入系统代理绕过列表（ProxyOverride），映射什么写什么；引擎启动时自愈同步，补回被代理软件（Clash 等）覆盖的条目；GUI 新增开关（默认开启），关闭时可选保留/清理已写入条目（#2）

## v0.2.1（2026-09-03）

- ci: release bump 步骤重跑前清理同名分支

